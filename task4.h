#ifndef TASK4_H
#define TASK4_H

#include <string>
using namespace std;

// Returns the index of the first occurrence of 'pattern' in 'text'
// using the naive (brute-force) matching algorithm.
// Returns -1 if the pattern is not found.
int findPattern(const string &text, const string &pattern);

#endif
